import { RoleCheckerDirective } from './role-checker.directive';

describe('ProjectPermissionCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
